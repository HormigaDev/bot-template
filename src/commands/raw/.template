import { EmbedBuilder, Colors, Client, Message } from 'discord.js';
import { Bot } from '@/bot';
import { RawCommand } from '@/types/RawCommand';

export const cmd: RawCommand = {
    name: '<name>',
    alias: [],
    async execute(bot: Bot, message: Message, args: string[]) {
        // Aquí el código del comando
    },
};
